# Copy to terraform.tfvars (gitignored) and fill in.

aws_account_id  = "123456789012"        # TODO: real AWS account ID
aws_region      = "us-west-2"
domain          = "aegischain.xyz"      # TODO: confirm domain
route53_zone_id = "Z0123456789ABCDEFGH" # TODO: hosted zone for `domain`

# image_tag is normally rolled by CI — leave default for first deploy.
# arweave_identity_tx_id is set after running scripts/upload-agent-identity.py.
